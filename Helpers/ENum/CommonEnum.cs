namespace WeatherApplication.Helpers.ENum
{
    public enum eUnit
    {
        Metric = 1,     // Celsius
        imperial = 2        //Fahrenheit
    }

    public enum eRequestType
    {
        current = 1,
        daily = 2,
        hourly = 3,
        minutely = 4
    }
}
